name: Build Android APK

on:
  push:
    branches: [main, master]
  workflow_dispatch:  # allow manual trigger

jobs:
  build-apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            git zip unzip openjdk-17-jdk python3-pip \
            autoconf libtool pkg-config zlib1g-dev \
            libncurses5-dev libncursesw5-dev libtinfo6 \
            libssl-dev libreadline-dev libffi-dev

      - name: Install Cython (for Android compilation)
        run: pip install --upgrade pip "Cython<3.0"

      - name: Install Buildozer
        run: pip install buildozer==1.5.0

      - name: Build APK (debug)
        run: buildozer android debug 2>&1

      - name: Show built APK
        run: ls -la bin/

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: bouncing-ball-simulator-apk
          path: bin/*.apk
